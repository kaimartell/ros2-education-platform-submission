window.__ROS2_BRIDGE_CLASSROOM_CONFIG__ = {
  // Leave blank only when the UI is served from the same HTTPS host as the proxy.
  // Set this explicitly when the UI is hosted somewhere other than the proxy.
  // Example: "wss://10.5.10.23/ws/"
  rosbridgeUrl: "",
};
